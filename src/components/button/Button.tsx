import React from "react";

const Button = () => {
  return (
    <div>
      <button className="bg-primary-400 px-10 py-2 mt-3 text-white rounded-full transition-all duration-300 hover:bg-primary-400/70">
        Join
      </button>
    </div>
  );
};

export default Button;
